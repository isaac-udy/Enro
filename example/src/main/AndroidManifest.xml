<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nav.enro.example">

    <application
        android:name=".ExampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="nav.enro.example.feature.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.DashboardActivity" />
        <activity android:name=".feature.DetailActivity" />
        <activity android:name=".feature.ListActivity" />
        <activity android:name=".feature.SearchActivity" />
        <activity android:name=".feature.UserActivity" />
        <activity android:name=".feature.MultiStackActivity" />
        <activity android:name=".feature.MasterDetailActivity" />

    </application>

</manifest>